<%= render "devise/shared/navbar" %>
<div><%= render 'devise/shared/search_form' %></div>

<div class="container">
<% @destinations.each do |destination| %>
  <h2>Your trip 🧳 details to visit <%= destination.name %> with <%= @spaceship.name %> 🚀 are below.</h2>
    <div class="card-trip">
      <div class="img"><%= cl_image_tag @spaceship.image_url.key %></div>
      <div class="card-trip-infos">
        <div>
          <h2>Rocket: <%=@spaceship.name %></h2>
          <p> Description: <%= @spaceship.description %></p>
          <p> Journey duration: <%= destination.flight_time %> days</p>
        </div>
        <div>
          <h2 class="card-trip-pricing">£<%= @spaceship.capacity * 100 - 0.01 %></h2>
          <p>Up to <%= @spaceship.capacity %> passengers</p>

        </div>
      </div>
    </div>
  <br>
  <br>


  <div class="card-destination">
    <div class="img"><%= cl_image_tag destination.image_url.key %></div>
    <div class="card-destination-infos">
      <h2><%= destination.name %></h2>
      <p>Prepare to blast off for a <%= destination.flight_time %> day journey</p>
    </div>
  </div>
  <%end%>
  <br>
  <br>


  <% if @spaceship.user == current_user %>
    <%= link_to "Edit spaceship", edit_spaceship_path, class: "btn btn-flat" %>
    <%= link_to "Delete #{@spaceship.name}",
                spaceship_path(@spaceship),
                method: :delete,
                class: "btn btn-flat",
                data: { confirm: "Are you sure?" } %>
  <% else %>
    <%= link_to "Make a booking", new_spaceship_booking_path(@spaceship), class: "btn btn-submit" %>
  <% end %>
</div>
